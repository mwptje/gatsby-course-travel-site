import React from "react"

// Note background images is from the
// layout.css
const SimpleHero = ({ children }) => {
  return <header className="defaultHero">{children}</header>
}

export default SimpleHero
